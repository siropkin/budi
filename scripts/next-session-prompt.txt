Continue with Phase 8 of the v4 pivot (CI & Release). Read memory/pivot_plan.md for the plan and memory/feedback_workflow.md for the phase completion checklist.

## Current state (v4.0.0, 2026-03-21)

- Branch: main, 6 commits ahead of origin
- All 45 tests pass (42 budi-core + 3 budi-daemon), 0 clippy warnings, fmt clean
- Installed: ~/.local/bin/budi + budi-daemon (v4.0.0)
- Phase 8 changes are staged but NOT yet committed

## What was done this session (Phase 8: CI & Release)

### 1. Fixed clippy error — `assert!(true)` in daemon tests
- Replaced placeholder with 3 real axum handler tests (health, stats, dashboard)
- Added `tower` and `http-body-util` dev-dependencies
- Extracted `build_router()` function for testability

### 2. Fixed bump-plugin-version.sh path
- Moved from `scripts/dev/bump-plugin-version.sh` to `scripts/bump-plugin-version.sh`
- Now matches the path referenced in `publish-marketplace.yml`

### 3. Cleaned up stale v3 RAG artifacts
- Removed `scripts/dev/` entirely (reranker_*, channel_scores.jsonl, training data)
- All v3 code preserved in `v3-rag-final` git tag

### 4. Added macOS CI job
- New `macos-build` job on `macos-14` (Apple Silicon) with build + test + smoke test
- CI now covers: Linux (fmt/clippy/test/build), macOS (build/test), Windows (build/test)

### 5. Added x86_64 macOS release target
- Release matrix now builds: linux-x86_64, macos-aarch64, macos-x86_64, windows-x86_64

### 6. Added daemon integration smoke test to CI
- Starts daemon on port 19876, hits /health, /stats, /dashboard, validates responses
- Covers the analytics endpoints added in phases 3-6

### 7. Created unified release script
- `scripts/release.sh` bumps Cargo.toml + Cargo.lock + plugin + marketplace in one command
- Optional `--tag` flag to create git tag

## Next steps

1. **Commit Phase 8** — review changes, commit with phase message
2. **Dashboard favicon** — add favicon similar to budi status line emoji
3. **Push & publish release** — push to origin, create v4.0.0 tag, trigger release workflow
4. **Dashboard UX/UI review** — research dashboard best practices, review layout/tiles/data hierarchy for clarity and simplicity
