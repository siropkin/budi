Read /Users/ivan.seredkin/.claude/plans/budi-improvement-loop.md and use it as the source of truth.
Continue the regular improvement loop. Follow the file's non-negotiables, benchmark-repo guidance, loop protocol, and validation rules exactly.

## Current state (v3.13.1, 2026-03-13)

- Branch: main, CI green (all workflows pass including Release)
- All 358 tests pass (333 budi-core + 21 budi-cli + 4 budi-daemon), 0 clippy warnings
- Installed: ~/.local/bin/budi + budi-daemon + budi-mcp (v3.13.1)
- Repo data dir clean: 15 real repos (8 benchmark + 7 wild-repos)

## What was done in session 27 (2026-03-13)

### 1. Graph self-reference bug fix — DONE
- Root cause: `extract_reference_tokens()` processed definition lines that matched
  `looks_like_reference_line()` (e.g., `def dispatch_request(self, ctx) -> ft.ResponseReturnValue:`
  has '.' and '('), extracting the definition name as a reference token
- This caused definition chunks to self-reference in `graph_token_to_chunk_ids`,
  then `diversify_channel_by_path` (1-per-file dedup) dropped the actual caller
  when both were in the same file
- Fix: skip `looks_like_definition_line()` lines in `extract_reference_tokens()`
- 5 new tests (reference_tokens_skip_definition_lines, graph_index_no_self_reference,
  call_token_extraction_self_method, call_sites_from_full_dispatch_request,
  graph_index_maps_self_method_caller_to_definition)
- Flask `full_dispatch_request` graph score: 0.0 → 0.1520
- All benchmark repos re-indexed with --hard

### 2. Benchmark validation results (v3.13.1)

| Benchmark | Non-reg | Rate | Notes |
|-----------|---------|------|-------|
| Django | 17/18 | 94% | P18 mild regression (judge noise, flow-trace) |
| Flask | 16/18 | 89% | P13/P14 mild (Q-1), same as baseline |
| Express | 16/18 | 89% | P14 mild (Q-1); P8/P9 big wins (sym-use 6→9!) |

Express improved from 83% → 89% (P8 went from regression to +3 win).

## Current benchmark state (v3.13.1, validated 3/8 repos)

| Benchmark | Non-reg | Rate | Cost | Notes |
|-----------|---------|------|------|-------|
| Django | 17/18 | 94% | -5% | P18 flow-trace mild regression |
| Flask | 16/18 | 89% | -4% | Same as baseline |
| Express | 16/18 | 89% | -15% | UP from 83%, P8/P9 sym-use wins |
| Fastify | (not re-run) | ~89% | | |
| React | (not re-run) | ~89% | | |
| FastAPI | (not re-run) | ~89% | | |
| Ripgrep | (not re-run) | ~87% | | |
| Terraform | (not re-run) | ~78% | | |

## Key analysis insights

- Graph self-reference affected all languages where definition lines match
  `looks_like_reference_line()` (Python '.' + '(', Rust '->', Go '.' + '(', etc.)
- The fix improves graph channel accuracy for same-file caller discovery
- sym-use queries still hit `sym_use_no_call_site_skip` when actual callers
  rank below top 3 (Flask P8: full_dispatch_request at rank 6)
- Express P8/P9 wins suggest the graph fix helped some callers break into top 3

## Priority queue for next session

### 1. sym-use no_call_site_skip relaxation — MEDIUM
- Current: checks top 3 candidates for `target_symbol(` call expression
- Problem: actual callers often rank 4-6 (e.g., Flask P8 full_dispatch_request)
- Options: (a) expand to top 5-6, (b) weight graph-channel hits higher in check,
  (c) check all candidates above score floor, (d) only skip when 0 candidates have call

### 2. Run remaining benchmarks (Fastify/React/FastAPI/Ripgrep/Terraform) — LOW
- All repos re-indexed with --hard, ready for benchmarking
- Graph fix is cross-cutting, may affect all repos

### 3. ContextBench evaluation — LOW
- External validation benchmark (66 repos, 8 languages)
- Would need adaptation for budi's query-based model

### 4. Session dedup analytics — LOW
- Log dedup count alongside skip reason for benchmark analysis

## Other backlog items
- Impact analysis / git-blame indexing
- Pre-trained code cross-encoder (needs 5-10K+ training pairs)
- Parallel chunking (rayon) for --hard rebuilds
- fastembed-rs v5.12+ has Qwen3 and BAAI/bge-m3 — evaluate if relevant
- Hook-io trace accuracy: investigate why benchmark reports ctx=0 when context IS injected
