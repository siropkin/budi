Read /Users/ivan.seredkin/.claude/plans/budi-improvement-loop.md and use it as the source of truth.
Continue the regular improvement loop. Follow the file's non-negotiables, benchmark-repo guidance, loop protocol, and validation rules exactly.

## Current state (post-v3.1.0, 2026-03-11)

- Branch: main, CI green, all 290 tests pass (265 budi-core + 21 budi-cli + 4 budi-daemon)
- Installed: ~/.local/bin/budi + budi-daemon + budi-mcp
- 0 clippy warnings
- v3.1.0 released + 6 post-release commits on main

## What was done in the last session

All committed and pushed to main, CI green:
1. 5 new language grammars: Ruby, Kotlin, Swift, Scala, PHP — 13 AST languages total (d9cdc88)
2. Definition boundary detection for new languages in retrieval pipeline (012176d)
3. Status line for Claude Code: `budi statusline` command shows live stats (d2b1210)
4. Indexing progress in status line: purple ⟳ indicator during active builds (e8e9a5b)
5. Auto-install status line on `budi init`, simplified display (a96475f)
6. Web research: Augment Context Engine MCP (GA), HTTP hooks, nomic-embed-code (7B)

## Priority queue for next session

### 1. Indexing robustness and performance (#37) — HIGH PRIORITY
The indexing pipeline needs a focused research + optimization pass:
- **Speed**: Profile hot paths, target <60s for 1000+ file repos
- **Lazy/incremental**: Only re-chunk changed files, soft re-index as default
- **Dirty file support**: Index uncommitted/unstaged changes (developers work on dirty files)
- **Git worktree support**: Share base index across worktrees, only re-index diffs — NO full re-index
- **Index size**: Audit SQLite + HNSW + embedding cache disk usage
- **Non-blocking**: Verify no deadlocks between index writes and queries, stress test
- **Research first**: Look at Augment Context Engine, GitNexus, Sourcegraph approaches

Start with research and profiling, then implement. This is the biggest technical risk for adoption on large real-world repos.

### 2. README overhaul (#38) — MEDIUM PRIORITY
Research top GitHub READMEs, replace mermaid charts with GIF/asciicast, better "why budi", keep concise.

### 3. Verkada-Web real-world validation (#39) — AFTER #37
Install on a large TS monorepo, collect stats over multiple sessions, analyze. Do this after indexing is robust.

## Other items still on the backlog
- HTTP hooks migration (eliminate CLI subprocess overhead)
- nomic-embed-code (7B) evaluation (better local embeddings)
- Benchmark against Augment Context Engine MCP
- Impact analysis / git-blame indexing
