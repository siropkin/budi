Read /Users/ivan.seredkin/.claude/plans/budi-improvement-loop.md and use it as the source of truth.
Continue the regular improvement loop. Follow the file's non-negotiables, benchmark-repo guidance, loop protocol, and validation rules exactly.

## Current state (v3.4.0+4, 2026-03-12)

- Branch: main, CI green (all workflows pass)
- All 314 tests pass (289 budi-core + 21 budi-cli + 4 budi-daemon), 0 clippy warnings
- Installed: ~/.local/bin/budi + budi-daemon + budi-mcp (v3.4.0+4)
- Repo data dir clean: 15 real repos (8 benchmark + 7 wild-repos)
- v3.4.0 released: https://github.com/siropkin/budi/releases/tag/v3.4.0

## What was done in session 15 (2026-03-12)

### 1. HNSW non-determinism fix — DONE (major)
- Configured ORT global thread pool with `intra_threads=1` before fastembed session creation
- fastembed hardcoded `available_parallelism()` → non-deterministic float reduction
- `init_ort_deterministic()` called in daemon startup before warmup_embedder
- Flask sym-def score variance: ±0.40 → ±0.005
- Cold-cache indexing ~2-3x slower (rare, worth the determinism tradeoff)

### 2. Test repo pollution fix — DONE
- Added `cleanup_test_repo()` helper that removes both temp dir and budi data dir entries
- Previously each `cargo test` created ~10 stale entries (80+ accumulated)

### 3. Doctor UX + HTTP hooks detection — DONE
- Optional ignore files only shown when present (no misleading `[!!]`)
- `detect_hooks()` now recognizes HTTP hook format

### 4. Research sweep — DONE
Key findings:
- Claude Code: 2 new hook handler types (`prompt`, `agent`). Bug fixes for double-fire and no-op injection.
- **ModernBERT-Embed** (Nomic): ONNX, 768d with Matryoshka to 256d. Worth benchmarking as AllMiniLM upgrade.
- **cAST paper** (arXiv 2506.15655): AST sibling merging improves chunking quality. Validates budi's approach.
- **HNSW fix**: ort exposes `with_intra_threads(1)` via GlobalThreadPoolOptions. Implemented above.
- Competitors: No threats. Augment (cloud MCP), SWE-grep (Windsurf-only), context-mode (complementary).

## Priority queue for next session

### 1. Benchmark revalidation with deterministic embeddings — HIGH
HNSW fix means single-run benchmarks are now reliable. Re-run React + Flask focused
to see if results stabilize. If regression rate drops, update benchmark numbers.

### 2. ModernBERT-Embed evaluation — MEDIUM
ONNX available, 768d Matryoshka to 256d, Apache-2.0. Similar size to AllMiniLM.
Would require re-indexing all repos but could improve retrieval quality.
Start with Flask focused benchmark (smallest repo).

### 3. Verkada-Web daily-driver usage — MEDIUM
Use budi for actual development work. Collect real usage stats.

### 4. Version bump and release — LOW
4 commits since v3.4.0. HNSW fix is significant. Consider v3.5.0.

### 5. Competitive A/B: budi vs Augment — LOW
Requires paid plan. Setup: npm install + auggie login + claude mcp add-json.

## Other backlog items
- cAST sibling-merging chunking heuristic (arXiv 2506.15655)
- Impact analysis / git-blame indexing
- Pre-trained code cross-encoder
- Parallel chunking (rayon) for --hard rebuilds
