Read /Users/ivan.seredkin/.claude/plans/budi-improvement-loop.md and use it as the source of truth.
Continue the regular improvement loop. Follow the file's non-negotiables, benchmark-repo guidance, loop protocol, and validation rules exactly.

## Current state (post-v3.1.0, 2026-03-11)

- Branch: main, CI green, all 292 tests pass (267 budi-core + 21 budi-cli + 4 budi-daemon)
- Installed: ~/.local/bin/budi + budi-daemon + budi-mcp (freshly copied from release build)
- 0 clippy warnings
- v3.1.0 released + 10 post-release commits on main
- 18 repos in ~/.local/share/budi/repos/ (cleaned from 128)

## What was done in the last session

All committed and pushed to main, CI green:
1. Indexing robustness (#37): incremental HNSW delta updates (2.5x faster), git worktree index sharing, timing instrumentation (bb2e707 + c0db6d6)
2. README overhaul (#38): restructured following top GitHub repo patterns — bold-noun feature bullets, comparison anchor tagline, mermaid charts removed, 171→112 lines (9e53e87)
3. Status line alignment (#40): padding 1→0 so budi aligns with Claude Code's built-in lines (5fef8f3)
4. Doctor UX fix: optional config.toml no longer shows [!!], shows "using defaults" instead (5e11a11)
5. Install docs fix: README curl-pipe-to-bash didn't work, fixed to clone-then-run (f4d98d6)
6. MCP docs: added MCP server section to docs/configuration.md
7. Repo hygiene: cleaned 110 stale test repos (128→18)

## Priority queue for next session

### 1. Demo GIF/asciicast for README — QUICK WIN
README has a placeholder `<!-- TODO: Add demo GIF/asciicast -->`. Record a terminal demo showing budi in action: init, query, injection, status line. Tools: asciinema or vhs (charmbracelet/vhs).

### 2. Verkada-Web real-world validation (#39) — MEDIUM PRIORITY
Install latest budi on a large production TS monorepo. Index, use for real dev work, collect stats (injection rate, accuracy, read hit rate, cost). The ultimate product readiness test. Do this now that indexing robustness (#37) is done.

### 3. Lightweight reranker (#32) — STRATEGIC
Small cross-encoder over top-N candidates could replace many hand-tuned score floors. 131-prompt benchmark dataset = ready-made eval set. Research: ms-marco-MiniLM-L-6-v2 (80MB ONNX) or code-specific alternatives.

## Other items still on the backlog
- HTTP hooks migration (eliminate CLI subprocess overhead)
- nomic-embed-code (7B) evaluation (better local embeddings)
- Benchmark against Augment Context Engine MCP
- Impact analysis / git-blame indexing
- Train retrieval weights from 131-prompt benchmark data
