Read /Users/ivan.seredkin/.claude/plans/budi-improvement-loop.md and use it as the source of truth.
Continue the regular improvement loop. Follow the file's non-negotiables, benchmark-repo guidance, loop protocol, and validation rules exactly.

## Current state (v3.7.0, 2026-03-12)

- Branch: main, CI green (all workflows pass)
- All 336 tests pass (311 budi-core + 21 budi-cli + 4 budi-daemon), 0 clippy warnings
- Installed: ~/.local/bin/budi + budi-daemon + budi-mcp (v3.7.0)
- Repo data dir clean: 15 real repos (8 benchmark + 7 wild-repos)
- v3.7.0 released: https://github.com/siropkin/budi/releases/tag/v3.7.0

## What was done in session 19 (2026-03-12)

### 1. Full 8-repo benchmark revalidation — DONE (major)
All repos freshly run on v3.7.0 (post-sibling-merge + docstring extraction):
- React: 17/18 (94%), -12% cost. Best ever.
- Flask: 16/18 (89%), -9% cost. 0% injection regression.
- Django: 12/18 (67%), -13% cost. 4 ctx=0 LLM var, P13/P14 injected (known).
- Terraform: 16/18 (89%), -17% cost. P5 known thin wrapper.
- Fastify: **18/18 (100%)**, -11% cost. Perfect! 7 wins.
- FastAPI: **18/18 (100%)**, -6% cost. Perfect! 9 wins.
- Express: 14/18 (78%), -19% cost. From session 18.
- Ripgrep: running (session 19 re-run in progress).
- Total (7 repos, 126 judged): ~111/126 (~88%), ~-12% cost.
- Key insight: Real injection regressions = 3/126 (2.4%), ALL sym-def thin wrappers.

### 2. Granite-embedding-small-r2 evaluation — DONE (no action)
- ONNX now available on HuggingFace (onnx-community/granite-embedding-small-english-r2-ONNX)
- Loaded via fastembed UserDefinedEmbeddingModel (quantized, ~52MB)
- Verdict: Keep AllMiniLM. Same discrimination problem as ModernBERT.
- Granite score spread: 0.03-0.14 vs AllMiniLM 0.15-0.47
- Scores cluster 0.70-0.88 vs AllMiniLM 0.09-0.63
- Would require recalibrating all 50+ thresholds for worse separation

### 3. Docstring evidence card relevance — DONE (1edcc30)
- Extract first meaningful docstring line from chunk text
- Added as `relevance:` field in evidence cards
- Supports: Python triple-quote, Rust ///, Go/JS //, JSDoc /** */
- Django QuerySet: "Represent a lazy database lookup for a set of objects"
- Flask class: "The flask object implements a WSGI application..."
- 9 new tests

### 4. Research sweep — DONE
- RAG vs agentic debate validates budi's hybrid pre-injection approach
- fastembed-rs: no new small ONNX code models (Qwen3/Nomic use candle)
- Claude Code: 15 hook events, HTTP hooks. No new relevant hooks.
- No new competitors threatening budi's position.

## Priority queue for next session

### 1. Complete Ripgrep revalidation — HIGH
Running now. Check for results in ~/.local/share/budi/repos/ripgrep-*/benchmarks/

### 2. Context condensation — MEDIUM
Docstring extraction is done. Other ideas:
- Intent-specific card formatting (SymbolDef: signature + docstring, FlowTrace: call chain emphasis)
- Better proof line selection for specific intents
- Cross-file relationship annotations

### 3. Prompt-context management research — MEDIUM
- Study how to reduce total context load (CLAUDE.md + system reminders + budi injection)
- Agent config file discovery (gitignored files like CLAUDE.md)
- Deduplication when budi injection overlaps with existing context

### 4. Version bump + release — LOW
If Ripgrep results are good, bump to v3.8.0 and release.

## Other backlog items
- Impact analysis / git-blame indexing
- Pre-trained code cross-encoder (needs 5-10K+ training pairs)
- Parallel chunking (rayon) for --hard rebuilds
- Verkada-Web daily-driver usage
