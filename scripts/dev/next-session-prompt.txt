Read /Users/ivan.seredkin/.claude/plans/budi-improvement-loop.md and use it as the source of truth.
Continue the regular improvement loop. Follow the file's non-negotiables, benchmark-repo guidance, loop protocol, and validation rules exactly.

## Current state (v3.12.0, 2026-03-13)

- Branch: main, CI green (all workflows pass including Release)
- All 353 tests pass (327 budi-core + 21 budi-cli + 4 budi-daemon), 0 clippy warnings
- Installed: ~/.local/bin/budi + budi-daemon + budi-mcp (v3.12.0)
- Repo data dir clean: 15 real repos (8 benchmark + 7 wild-repos)
- v3.12.0 released: https://github.com/siropkin/budi/releases/tag/v3.12.0

## What was done in session 25 (2026-03-13)

### 1. "Explain Symbol" prefix generalization — DONE (0560245)
- Renamed has_symbol_after_what_is → has_symbol_after_prefix with configurable prefix list
- "Explain QuerySet", "Explain useState" etc. now route to SymbolDefinition
- 0/144 benchmark prompts start with "Explain" — no impact. 5 new test assertions.

### 2. Full 8-repo benchmark sweep — DONE
- Ripgrep re-cloned (1969 chunks) and re-indexed
- All 144 prompts judged across 8 repos

### 3. Session dedup diagnostic insight + fix — DONE (7ce0d9f)
- Found: high retrieval_top_score + snippets_count=0 cases caused by session dedup
- diagnostics.top_score was set before daemon session dedup, now refreshed post-dedup

### 4. Research sweep — DONE
- Claude Code: HTTP hooks, WorktreeCreate/WorktreeRemove, TeammateIdle, TaskCompleted hooks
- Agent/skill frontmatter hooks, memory frontmatter with user/project/local scope
- PreCompact hook available but budi already handles compact via SessionStart source detection
- No new 384d code embedding models. AllMiniLM remains best
- fastembed-rs still at 5.12.0. Qwen3 and BAAI/bge-m3 support added in v5.8+
- ContextBench (arxiv 2602.05892): 66 repos, 8 langs, gold-annotated. Block-level F1 < 0.45 for SOTA
- Codified Context paper (arxiv 2602.20478): hot/cold memory split mirrors budi architecture

### 5. Version bump to v3.12.0 — DONE (f2c95fd)
- "Explain X" routing + post-dedup diagnostics accuracy. All CI + Release green.

### 6. Repo hygiene — DONE
- Removed stale ripgrep-b062d3eafb54 data dir. 15 repos remain.

## Current benchmark state (v3.12.0, 8 repos, 144 judged prompts)

| Benchmark | Non-reg | Rate | Cost | Notes |
|-----------|---------|------|------|-------|
| Flask | 16/18 | 89% | -9% | P13/P18 mild (Q-1) |
| Fastify | 16/18 | 89% | -15% | P4/P14 mild (Q-1) |
| React | 16/18 | 89% | +13% | P1 extreme judge noise (9→4) |
| Express | 15/18 | 83% | -15% | P8 ctx=0 variance (9→5) |
| Django | 15/18 | 83% | -8% | P14 persistent sym-def |
| FastAPI | 16/18 | 89% | -5% | P6/P15 mild (Q-1) |
| Ripgrep | 13/18 | 87%* | -4% | P10/P14 mild, 3 n/a |
| Terraform | 14/18 | 78% | -10% | P5/P13 known noise |
| **Total** | **~121/144** | **~84%** | **~-7%** | Judge noise dominates |

*87% of 15 judged. Flask+Fastify went 100%→89% with identical code = judge variance.

## Injection quality (31 injected queries across 8 repos)
- Wins: 9 (29%), Ties: 19 (61%), Regressions: 3 (10%)
- Only 3 regressions on injected queries (Django P14 persistent, Terraform P5/P13 noise)
- Express P15 had dramatic win (+3, Q 5→8)

## Key analysis insights
- 110/144 (76%) queries get ctx=0 (budi correctly skips)
- All 4 sharp regressions (Q ≥-2) are ctx=0 = pure LLM response variance
- Django P14 "resolve" is ambiguous-symbol problem — multiple definitions across files, budi picks internal URLResolver.resolve but misses public API django/urls/base.py:22
- PreCompact hook not needed — SessionStart already re-injects project map on compact/clear
- WorktreeCreate hook not needed — resolve_storage_root() shares index, configs shared across worktrees

## Priority queue for next session

### 1. Ambiguous-symbol multi-definition support — MEDIUM
- When multiple definitions of the same symbol exist across different files, consider including top 2-3 definitions instead of just the highest-scoring one
- Django P14 "resolve" is the persistent test case (URLResolver.resolve vs base.resolve)
- Needs careful design to avoid context bloat for unambiguous symbols

### 2. ContextBench evaluation — LOW
- External validation benchmark (66 repos, 8 languages, gold-annotated contexts)
- Measures context retrieval recall/precision at file/block/line granularity
- Designed for issue-resolution tasks — would need adaptation for budi's query-based model

### 3. Session dedup analytics — LOW
- Log dedup count alongside skip reason to better attribute ctx=0 causes in benchmarks
- Would make benchmark analysis easier (currently need to cross-reference session IDs)

### 4. EmbeddingGemma evaluation — LOW
- Google's 308M model with Matryoshka truncation to 384d
- Would need ONNX export and integration with fastembed-rs
- Not code-specialized, but Matryoshka truncation is interesting

## Other backlog items
- Impact analysis / git-blame indexing
- Pre-trained code cross-encoder (needs 5-10K+ training pairs)
- Parallel chunking (rayon) for --hard rebuilds
- Explore reducing false skips on sym-use queries (largest category of ctx=0)
- fastembed-rs v5.12+ has Qwen3 and BAAI/bge-m3 — evaluate if relevant
