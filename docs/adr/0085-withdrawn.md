# ADR-0085: Withdrawn

- **Status**: Withdrawn (number never landed)

This number was reserved during drafting in the 8.0–8.1 window and never moved into the repo. No decision, context, or consequence text exists for it, and per the numbering policy in [`README.md`](./README.md) the slot is not reused.

This placeholder exists so that:

- The directory listing has no unexplained gaps when readers eyeball `ls docs/adr/`.
- A future contributor does not silently re-allocate `0085` to a different decision.
- The withdrawn-but-reserved status is discoverable from the file tree, not only from a buried paragraph in the index.

If the original draft is recovered from a contributor's local notes, it should be re-numbered to the next available ADR number rather than backfilled here.
